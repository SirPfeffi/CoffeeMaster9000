<AdminScreen>:
    name: "adminscreen"

    canvas.before:
        Color:
            rgba: app.root.ids.theme.background
        Rectangle:
            pos: self.pos
            size: self.size

    BoxLayout:
        orientation: "vertical"
        spacing: dp(20)
        padding: dp(40)

        Label:
            text: "Adminbereich – CoffeeMaster 9000"
            color: app.root.ids.theme.text
            font_size: "24sp"
            bold: True

        GridLayout:
            cols: 2
            spacing: dp(10)
            size_hint_y: None
            height: self.minimum_height

            Button:
                text: "Benutzerverwaltung"
                background_color: app.root.ids.theme.primary
                color: [1, 1, 1, 1]

            Button:
                text: "Kassensaldo anzeigen"
                background_color: app.root.ids.theme.primary
                color: [1, 1, 1, 1]

            Button:
                text: "Einzahlung buchen"
                background_color: app.root.ids.theme.success
                color: [1, 1, 1, 1]

            Button:
                text: "Fehlbuchung stornieren"
                background_color: app.root.ids.theme.error
                color: [1, 1, 1, 1]

        Button:
            text: "Zurück zum Hauptbildschirm"
            background_color: app.root.ids.theme.primary
            color: [1, 1, 1, 1]
            size_hint_y: None
            height: dp(50)
            on_release: app.switch_to_main()
